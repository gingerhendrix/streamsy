---
title: Concepts
description: Understand outcomes, typed faults, batch boundaries and memory-store ownership.
---

## Outcomes and faults

Protocol success results contain only accepted states. Create returns `Created` or
`Exists`; append returns `Appended` or `Duplicate`, discriminated by `_tag`. Head
and reads return their data directly; remove returns `void`. Long-poll batches add
`timedOut: boolean`, and `closed` is always boolean.

Expected protocol failures use Effect's error channel. All carry `id` and can be
handled with `Effect.catchTag`, such as `StreamNotFound`, `StreamGone`,
`OffsetMismatch`, or `AppendConflict`. `Streams.read`, `Streams.follow`, and
`Streams.session` preserve these errors. `StorageFault` carries infrastructure
retryability; remote infrastructure uses `TransportFault`.

Retries need a bounded policy and retained intent. Producer duplication
acknowledges a tuple; it does not compare retry payloads. The caller must preserve
the same bytes, epoch and sequence across a retry.

Encoding and decoding failures use `EncodeFault` and `DecodeFault`. A decode
failure stops that consumer; choosing a new offset or skipping bad input is the
application's policy. The separate storage contract reports an expected mutation
rejection as `MutationRejected`, and it rolls the transaction back.

## Batches and offsets

A batch contains decoded `items`, `nextOffset`, `upToDate` and `closed`.
`Streams.items` flattens a stream of batches when only values are needed.
Keep batches when a consumer needs their resume position.

Offsets are opaque, fixed-width tokens. Store and forward them unchanged, and
compare positions only within one stream. `ZERO_OFFSET` denotes an empty tail.
An append's `expectedOffset` checks the current tail atomically with the mutation.
A mismatch returns `conflict` with the actual position and writes nothing.
This compare-and-swap (CAS) check detects competing writes; it does not establish
an application session's ownership policy.

## Storage capabilities

The memory Layer has two tested configurations:

| Capability   | Default | Constrained |
| ------------ | ------- | ----------- |
| Fork         | Chain   | Copy        |
| Atomic scope | Store   | Stream      |
| Wake         | Push    | Poll        |
| Expiry index | Indexed | Lazy        |

Multi-stream mutations require store atomicity. A single mutation naming the
same stream twice, or a multi-stream mutation submitted to a stream-atomic store,
is a programmer defect. Read-time expiry works in both configurations. The Bun host expires on
access; it does not promise a background expiry sweep.

## Runtime ownership

Libraries return Effects and Streams. Applications run them at an executable
edge and own scopes, listeners and subscriptions. One acquired memory Layer is
one store; dispose it and the data is lost. The Bun host owns its HTTP edge and
listener, while an application that shares protocol services must acquire them
once for that application's lifetime.

The SQL Layer has store-wide atomicity, chain forks, push wakes with repair, and
an indexed expiry query. See [SQL storage](/docs/user/sql-storage). The local
Cloudflare entry provides the Durable Object protocol host and placement
selection; hosted execution and release evidence remain pending. See
[Cloudflare hosting](/docs/user/cloudflare-hosting).
