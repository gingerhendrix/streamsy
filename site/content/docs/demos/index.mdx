---
title: Demos
description: Runnable examples of Streamsy's HTTP edge and projection toolkit.
---

[Memory server](/docs/demos/memory-server) is the smallest HTTP host: a Bun
server, one process-local memory store, and a six-step `curl` walkthrough.

[Hacker News newest](/docs/demos/hackernews-newest-stream) polls stories, appends
source changes, wakes a declared-output `@streamsy/projection` through the
same-owner change feed, and serves State facts to a live browser collection.
Its offline smoke proves the change wake and checkpoint resume across a process
restart.

[Issue tracker](/docs/demos/issue-tracker-demo) is a complete stream-backed
application: Promise-native Bun routes transact against workspace streams, and
the browser replays each workspace directly into StreamDB.

[Issue tracker projection](/docs/demos/issue-tracker) feeds five workspace
inputs into fused SQL and named-output family members, serves rows, transitions
and a compact document, and serializes request and change triggers by member key.

[Hex board](/docs/demos/hex-board) keeps a complete board as fold state in the
checkpoint transaction, with SQLite restart and failed-step persistence tests.
